# Prefabs

The starter scene creates primitive placeholders at runtime so it can import without binary assets. Use this folder for project-specific core, enemy, weapon, and projectile prefabs after replacing the generated placeholders.
